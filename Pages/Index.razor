@page "/"
<PageTitle>Index</PageTitle>

<DashboardPage />

@code {
    bool visible = false;

    private void ToggleDropDown()
    {
        Console.WriteLine(visible);
        visible = !visible;
    }
}
